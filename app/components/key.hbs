<div class="p-6 border rounded shadow bg-white space-y-4" tabindex="0">
  <h2 class="text-2xl font-bold">🎹 Keyboard Control Panel</h2>

  <p class="text-gray-600 italic">{{this.status}}</p>

  <ul class="list-disc list-inside text-gray-700">
    <li><kbd>Escape</kbd> — Close modal or App exit</li>
    <li><kbd>Ctrl+S</kbd> — Save</li>
    <li><kbd>Ctrl+H / Ctrl+Shift+H</kbd> — Help</li>
    <li><kbd>ArrowUp / ArrowDown</kbd> — Navigate</li>
    <li><kbd>Ctrl+D</kbd> — Toggle Debug</li>
  </ul>

  <div class="text-lg font-mono text-blue-600 border rounded bg-gray-50 px-3 py-2">
    {{this.message}}
  </div>

  <div class="space-x-2">
    <button class="px-4 py-2 bg-blue-600 text-white rounded" type="button" {{on "click" this.clearMessage}}>
      🔄 Clear
    </button>

    <button class="px-4 py-2 bg-gray-800 text-white rounded" type="button" {{on "click" this.toggleActivation}}>
      {{if this.isActive "🛑 Deactivate" "🎧 Activate"}} Keyboard
    </button>

    <button class="px-4 py-2 bg-purple-600 text-white rounded" type="button" {{on "click" this.toggleModal}}>
      {{if this.inModal "📤 Close Modal" "🪟 Open Modal"}}
    </button>
  </div>
</div>
